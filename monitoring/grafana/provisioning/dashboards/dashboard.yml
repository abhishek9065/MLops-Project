# Tell Grafana to load any dashboard JSON files from the folder below.
apiVersion: 1

providers:
  - name: "Churn dashboards"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
