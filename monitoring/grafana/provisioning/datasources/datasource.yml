# Auto-provision the Prometheus datasource so Grafana works on first boot
# (no manual clicking in the UI).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090   # service name from docker-compose
    isDefault: true
    editable: true
